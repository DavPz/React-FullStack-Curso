

const average = 5.5;

const statusR = (average >= 5.5) ? 'Aprobado':'Rechazado';

console.log(`Resultado: ${statusR}`);

let max = 0;

const a = 15;
const b = 20;
const c = 13;

max = a > b ? a : b 
max =  max > c ? max : c;

console.log(`El numero mayor es ${max}`);
